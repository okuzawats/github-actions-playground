if git.lines_of_code > 400
  warn("Oops, this PR looks too big🤔Could you split it into some smaller PRs?")
else
  message("#{git.insertions} lines has inserted and #{git.deletions} lines has deleted! Looks good, happy coding🤖")
end
